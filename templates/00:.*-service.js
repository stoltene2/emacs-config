angular.module('${1:`js-proj-prefix-module`}')
  .service('$1.${2:$(s-upper-camel-case yas-text)}', [
    function () {
      'use strict';

      function ${2:SomeClass}() {
        $0
      }

      $2.prototype.someMethod = function () {

      };

      //////////////////////////////
      function myPrivateFunction(){ }

      return $2;
    }
  ]);
